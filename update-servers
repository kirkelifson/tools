#!/bin/bash

servers=(andromeda apollo euclid centauri aws)
for server in ${servers[@]}
do
    echo -e "\n[+] Connecting to $server..."
    ssh -o ConnectTimeout=5 -t $server "~/bin/update-server"
done
